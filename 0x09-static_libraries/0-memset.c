#include "main.h"
/**
 * _memset - This function is still booting
 * @s: a argument
 * @b: an argument
 * @n: an argument
 * Return: success alwayas returns 0.
 */
char *_memset(char *s, char b, unsigned int n);
