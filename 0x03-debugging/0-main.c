#include "main.h"
/**
 * main - tests function thatprints if integer is posi * tive or negative
 * Return: success always 0.
 */
 int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
