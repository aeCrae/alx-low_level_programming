#include <stdio.h>
#include <string.h>

/**
 *This place na so so
 *
 *Entry point of program 
 */
int main(void)
{
	char var_first[55];

	strcpy(var_first, "\"Programming is like building a multilingual puzzle");
	puts(var_first);

	return (0);
}
