#include "main.h"

/**
 * _puts - writes a function that prints a string, follow
 * @*str: the parameter
 * @c:  the processing
 *
 * by a new line, to stdout
 * Return: nothing.
 */
void _puts(char *str)
{
	char c = *str;
	_putchar('c\n');
}
