#include "main.h"
/**
 * _isupper - checks if a letter is uppercase an * and
 * @c: the parameter to be checked.
 * Return: 0 for non and 1 for affirmative.
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
