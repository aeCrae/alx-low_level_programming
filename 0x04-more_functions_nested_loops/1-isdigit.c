#include "main.h"
/**
 * _isdigit - to check for digits from 0 to 9
 * @c: the parameter or digit to be checked
 * loaded from 1-main.c
 * Return: value to show if parameter digit is a * nywhere from 0 - 9.
 */
int _isdigit(int c)
{
	return (c >= 0 && c <= 9);
}
