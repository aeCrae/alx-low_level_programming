#include "main.h"
/**
 * create_array - a function to create an array of chars
 * @size: an argument which sends the arrays size
 * @c: another argument, not sure yet.
 * Return: null if size is 0, returns a pointer to the arry
 */
char *create_array(unsigned int size, char c);
